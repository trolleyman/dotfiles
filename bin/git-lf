#!/bin/bash

git diff --quiet || (echo 'error: dirty git repo'; exit 1)

set -ex

# Set correct crlf settings
git config core.autocrlf input
git config core.eol lf

# Reset current files
git ls-files -z | xargs -0 rm
git checkout .

